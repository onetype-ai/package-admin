config.Item({
	key: 'admin.screens.active',
	description: 'ID of the open screen, null while the normal shell shows.',
	value: null,
	config: {
		type: 'string',
		value: null,
		description: 'ID of the open screen, null while the normal shell shows.'
	}
});
